pipeline{
    agent any
    environment {
        VERSION = '1.0.0'
        RELEASE_VERSION = '2.3.4'
    }
    stages{
        stage('tools'){
            steps{
                dir
                java --version
                git --version
                mvn --version 
            }
        }
    }
}
   
